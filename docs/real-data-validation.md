# 验证基线与性能测量

本页集中记录已验证版本、覆盖范围和计时口径；不是验收规则副本。
规则见[验收规范](snapshot-validation-rules.md)，执行方法见[使用手册](Guidance.md)。

## 固定版本与覆盖范围

截至 2026-09-08，历史十五日全量代码对应提交 `f3f77c0`，Cargo 包版本 `0.1.0`。
清理未推送报告历史前，该提交为 `73adb97`；仅移除报告树，生产源码不变。
本地冻结 manifest 中保留原提交号和指纹，不为此次历史整理改写证据。
这是验证基线，不代表已创建 release tag。两批使用同一冻结 profiling 二进制，SHA256：

```text
9989b2cc307c518438a03731226d23644a7d1a391a6c4e16ee5a5ff3bc23d335
```

首批在提交前保存源码快照和工作树差异，随后代码提交为上述版本；第二批核对指纹一致。
不能只引用首批 manifest 的基础 HEAD 而忽略源码快照，也不能默认后续改动继承这些结果。

15 个交易日均完成沪深全部支持股票和 ETF 的逐笔恢复及 raw snapshot 验证：

- 原七日：20260320、20260401、20260601、20260616、20260706、20260806、20260828。
- 首轮随机：20260323、20260414、20260527。
- 第二轮随机：20260127、20260205、20260225、20260311、20260813。

| 批次 | 日市场任务 | 匹配项 | 不匹配 | 状态排除 | 数据错误／缺源 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 十日回归 | 20 | 277,909,184 | 0 | 381 | 0／0 |
| 五日扩展 | 10 | 136,804,964 | 0 | 172 | 0／0 |

共 414,714,148 个可比项全部匹配，553 个状态排除项不算匹配。
股票／ETF、三个阶段的分类、排除原因、输入清单、执行收据及指纹保存在本地 `reports/`。
十日与五日报告目录分别为 `20260907-current-full-regression/`、
`20260908-additional-random-full/`。报告由 Git 忽略，克隆仓库不包含这些详细证据；
需从单独备份取得，或使用相应版本和原始输入重新生成，不能把本文摘要等同于原始证据。

输入为 `/hdd/data/stock/raw_level2_parquet`，具体逐笔和 raw snapshot 数据集见
[使用手册](Guidance.md#输入与证券范围)。参考价量不参与订单簿恢复。
两批均采用当前默认窗口、无诊断覆盖；深市市价单使用 RestAtLastTradePrice，
不等于 RequireEvidence 严格待决策略通过，不能与 `is_standard_acceptance()` 混为一谈。

## Legacy 移除回归与证据边界

旧 QTP legacy 接口已移除，生产恢复／验证算法未改。golden 已拆成两个独立场景文件，
事件序列和预期值不变。上述十五日结果属于接口移除前版本；移除后的回归独立存证如下。

该阶段通过 123 项全部 target／feature 测试（golden 从一项拆为两项）、严格 Clippy、
格式、文档测试与 API 文档构建，以及使用手册的核心 Rust 示例。
C++ oracle 及其 CI 检查已移除，固定预期值和 Rust 回归覆盖保留。
生产恢复／验证源文件和 OrderBook 实现字节未变；移除的是旧输入接口而不是算法策略。

20260828 沪深股票与 ETF 全量重跑已于 2026-09-08 完成，两个市场均通过，
结果与移除前同日基线无差异。独立冻结二进制 SHA256 为
`25e9f31abbfd6cd2ba830b0020733f2e50a85a006fabb9f4c795946d59fd6e11`；
本地 `reports/20260908-legacy-retirement/` 中的 `summary.json` 和 `manifest.json`
保留结果及来源，不随 Git 分发。

| 市场 | 匹配项 | 不匹配 | 状态排除 | 数据错误／缺源 | 恢复秒 | 对比秒 | 核心计时总秒 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| SH | 12,342,471 | 0 | 5 | 0／0 | 616.72 | 4,855.62 | 5,472.33 |
| SZ | 13,881,870 | 0 | 18 | 0／0 | 2,025.48 | 4,384.91 | 6,410.39 |

该次为两进程并发，计时按下文 profiling 口径，核心计时不含报告序列化写出。
共 26,224,341 个可比项匹配、23 个状态排除项；同日重跑不增加十五日基线的独立日期数，
也不混加到上表旧二进制的匹配总数。该批次只覆盖当时冻结源码；后续优化独立验收，
不能将历史结果直接当作当前代码的结果。

- 已知沪市停牌选帧／历史 ETF 收盘阶段、深市委托逆序、V0 后限价单隐藏、
  无涨跌幅限制 E0 展示范围和 ETF 盘中窗口问题均在上述范围内闭合。
- 本地 `reports/diagnostics/` 的七份紧凑诊断保留原因证据；旧中间错误不代表当前失败。
  当前实现及仍存在的市价单中间态风险见[实现说明](order-book-implementation.md)。
- 跨日通过支持当前数据源窗口，不证明所有日期的最大延迟，不允许失败后自动扩窗。
  静态／周期 snapshot 通过不单独证明订单身份、全部逐订单余量和 FIFO。
- 仍保留核心单元、属性、固定 golden 和合成 Parquet 测试；CI 不依赖本机 `/hdd`。
  文档整理本身不扩大已验证范围；不同二进制的验收结果按上述批次分别追溯。

## P0/P1 优化回归

P0 提交 `347027b` 已重跑上述十五日、30 个日市场任务，全部语义报告与历史基线相等。
其冻结二进制 SHA256：
`0c46247747b49bac0da090fa749593ba6af5a24db9885599d6fcd3d167f04994`。
证据位于 `reports/20260909-p0-optimized-full-regression/`。

本次 P1 保持恢复和匹配规则不变，实施三项优化：

1. 候选先检查总量、加权价和成交统计。仅当标量差异已不可能改善最佳失败候选时，
   跳过十档读取；首次候选仍完整比较，收盘特殊规则仍完整检查。
2. 每个输入流缓存交易日零点，严格解析 `HH:MM:SS.mmm`；参考 Parquet 仅投影必要列，
   每个 RecordBatch 一次绑定 Arrow 类型，不逐行查列名／类型。
3. 参考十档以定长价量和 `u32` 委托数紧凑保存；失败诊断按需分配，匹配后释放。
   对外快照／报告格式及数量范围不变，参考委托数对应源数据 `UInt32`。

P1 以冻结 P0 为性能和结果基线，固定六进程并发、相同输入日期与运行参数。
独立证据目录为 `reports/20260909-p1-optimized-full-regression/`，运行结果见其
`manifest.json`、`summary.json`、`comparison.json` 及逐任务收据。
两批均省略成功逐帧明细；报告相等不等于逐条比较成功候选的时间和序号。
首次命中和最佳失败候选由差分测试逐候选对照完整比较路径验收。

2026-09-09 P1 全量回归已完成：30/30 任务通过，30 份 JSON 语义报告与 P0 完全相等。
414,714,148 个可比项全部匹配，553 个状态排除项不计入匹配；不匹配、数据错误及缺源均为零。
沪深各十五日的全部支持股票和 ETF 均在范围内，仍使用默认窗口和实用市价策略。
提交 `72b7d43` 的 Rust 源码与本批次冻结源码逐文件一致，输入清单（路径、大小、mtime、行数、Schema）
与 P0 相同。P1 冻结二进制 SHA256：
`470fe24282b1c87be0be6be1887ecbc6379c69189f2c4d10d50242f4993572c0`。

下表耗时为 30 个日市场任务的累计分项秒数；RSS 为各进程峰值的平均值，不是并发总峰值。

| 分项 | P0 | P1 | 下降 |
| --- | ---: | ---: | ---: |
| validation 总耗时 | 22,970.84 s | 16,277.08 s | 29.14% |
| 其中：参考快照加载 | 8,296.13 s | 2,019.72 s | 75.65% |
| 其中：验证回调 | 14,372.60 s | 14,029.44 s | 2.39% |
| 其中：报告整理 | 302.11 s | 227.92 s | 24.55% |
| 恢复链路（不含验证） | 21,906.65 s | 16,796.94 s | 23.32% |
| 平均峰值 RSS | 18.75 GiB | 14.42 GiB | 23.10% |

沪市 validation 累计耗时下降 29.95%，深市下降 28.34%；30 个任务的 validation 总耗时
均下降。整批并发日历耗时从 137.95 分钟降至 102.66 分钟，不能与累计分项耗时混用。

评估结论：组合优化有效，主要观察到参考加载、输入处理和内存占用改善。
验证回调仍占 P1 validation 耗时的主要部分：本轮跳过 455,677,012 次十档读取，
实际构造 4,340,116,722 次，占二者之和的约 9.50% 被跳过。
保留最佳失败候选的保守剪枝不能跳过全部标量不匹配；回调仅有 22/30 个任务更快，
沪市累计仅改善 0.89%，深市改善 3.79%。因此回调收益应视为小幅变化，不能宣称稳定的
显著加速，也不能把组合收益独立归因于某一项优化；后续仍应重点分析未被跳过候选的构造与比较。

这些是相同六进程配置下的两批 instrumented wall time，并非独占 CPU 或多轮随机交错 A/B。
缓存、调度及系统负载会影响结果，不能据此承诺其他硬件的固定加速比。
本轮通过 130 项 all-target/all-feature 测试、默认 feature 测试、文档测试、严格 Clippy、
`cargo fmt --check` 及维护脚本的 Ruff 检查；新增差分、时间边界、紧凑存储和列绑定拒绝路径覆盖。

## P1 validation 回调优化

以 `72b7d43` 的上述 P1 结果为独立基线，本轮优化不改变匹配窗口、加权价格容差、
最佳失败候选及收盘审计规则：

1. 复用标量差异掩码，直接比较紧凑参考档位；只为失败诊断或收盘特殊处理展开参考视图。
   验证加权价格仅计算一次，生产截面的价格计算不变。
2. 一次观察直接遍历当前参考帧切片，使用帧引用更新状态，不再按已知时间反复二分查找。
3. 每个标的保存一个候选视图，按盘口修订号复用标量及已读取十档。成功 `apply` 和
   `rest_pending_order` 均推进内部修订号，失败不推进；修订号饱和则禁用复用，不能回绕。
   修订号不是行情序号，不输出、不排序，也不改变最后成功事件元数据。

缓存仅在一个 BookKey／OrderBook 生命周期内使用，不缓存“是否可比较”或“是否匹配”；
窗口推进、继承状态和每条参考帧的首次命中时间仍逐次处理。MarketClose 绕过普通缓存，
其投影或 LastPrice 规范化不得修改普通候选视图。候选逻辑集中于
`src/production/validation/candidate.rs`，生产热路径不使用测试保留的时间查找接口。

小样本串行 ABBA 与十五日全量回归分别存于本地
`reports/20260909-p1-callback-abba/`、`reports/20260909-p1-callback-full-regression/`。
前者用于初步对照，后者逐任务核对旧报告并保存冻结 Rust 源码、输入清单和计时。
两批均已完成，最终状态和验收门槛见各 manifest。

20260828 四个样本的串行 ABBA 已完成，八次语义报告均一致；每个市场均为同一次请求
包含一只股票和一只 ETF。两次旧版／两次新版的回调秒数如下：

| 市场与样本 | 旧版两次 | 新版两次 | 均值下降 |
| --- | --- | --- | ---: |
| SH：600519、510300 | 0.5946、0.5967 | 0.3208、0.3185 | 46.34% |
| SZ：000001、159915 | 1.0982、1.0938 | 0.5473、0.5429 | 50.26% |

ABBA 仍是小样本 instrumented wall time，不用于承诺全市场或独占 CPU 的加速比。
全量回归除报告相等，还要求 `observation_calls`、`scalar_rejected_candidates` 与基线
逐任务完全相等；降低的是实际取值／查找／比较成本，不是观察次数或放宽剪枝条件。

2026-09-09 全量回归完成：30/30 个日市场任务通过，30 份语义报告与 `72b7d43` 完全相等。
414,714,148 个可比项全部匹配，553 个状态排除项保持不变；不匹配、数据错误及缺源均为零。
提交 `0b4c37a` 的 Rust 源码与本批次冻结源码逐文件一致，输入清单与基线相同。冻结二进制 SHA256：
`eb4477b998c8ccb1617d4cca0b95e93ee24376376be389cd34d44fb014706df4`。

同样使用六进程并发；下表是 30 个任务累计分项秒数，RSS 为单进程峰值的平均值：

| 分项 | 优化前（72b7d43） | 回调优化后 | 变化 |
| --- | ---: | ---: | ---: |
| validation 总耗时 | 16,277.08 s | 10,767.86 s | 下降 33.85% |
| 其中：验证回调 | 14,029.44 s | 8,483.52 s | 下降 39.53% |
| 其中：参考快照加载 | 2,019.72 s | 2,047.96 s | 增加 1.40% |
| 其中：报告整理 | 227.92 s | 236.38 s | 增加 3.71% |
| 恢复链路（不含验证） | 16,796.94 s | 16,153.77 s | 下降 3.83% |
| 平均峰值 RSS | 14.421 GiB | 14.435 GiB | 增加 0.10% |

沪市／深市回调耗时分别下降 42.67%／36.51%，validation 总耗时分别下降 36.14%／31.63%；
30 个任务的两项耗时均下降。整批日历耗时从 102.66 分钟降至 84.36 分钟。
主要收益在验证回调，不能把恢复分项的变化直接归因于恢复算法优化；内存基本持平。

共 16,895,737,088 次观察回调、455,677,012 次标量预筛跳过，与基线逐任务相等。
实际十档构造从 4,340,116,722 次降至 2,444,052,899 次，下降 43.69%；
候选视图缓存命中 2,134,224,602 次。缓存命中仍执行该参考帧的比较和时间窗口逻辑。

本轮通过 133 项 all-target/all-feature 测试、默认 feature 测试（132 项）、文档测试、
严格 Clippy、`cargo fmt --check` 及维护脚本的 Ruff 检查。新增覆盖紧凑字段比较与完整比较
等价性、同毫秒盘口变化、pending 重新入簿、失败不失效、修订号饱和、逐帧首次命中及收盘缓存隔离。
全量报告仍省略成功逐帧元数据，不能把报告相等表述为逐条核对所有成功候选时间；
首次命中与最佳失败诊断由单元／差分测试补充验收。
上述性能为同配置的两批 instrumented wall time，附小样本串行 ABBA，
不是独占 CPU、多轮全量随机交错基准，也不证明三项优化各自的独立收益。

## P1 分配与查表优化

以 `0b4c37a` 的十五日回调优化结果为独立基线，按顺序实施：

1. `Symbol` 内部改为共享不可变字符串，公开构造、内容比较、哈希、排序及 Debug 格式不变。
   `apply` 成功后接管事件元数据，删除沪市新增事件的多余克隆；spool 证券代码使用内联六字节，
   不改变 QTP1 字节布局及原有读取／写入拒绝边界。
2. validation 使用借用键入口一次定位标的，成交更新和序号审计复用同一状态；
   每日限价信息移到标的状态，不逐事件另查表。新增直接依赖 `hashbrown 0.16.1`，
   它原已存在于 lockfile；继续使用标准 `RandomState`，不更换为固定或非抗碰撞哈希。
3. 参考加载合并每标的阶段追踪、参考帧及限价元数据，只在新标的出现时分配键。
   限价来源只在首次记录或诊断需要时格式化；每行仍检查字段一致性，保留首次来源和原错误文字。

三个累计阶段分别运行 20260828 四标的串行 ABBA，证据目录为
`reports/20260909-p1-allocation-stage{1,2,3}-abba/`；全量批次为
`reports/20260909-p1-allocation-full-regression/`。各阶段均与同一旧版本对照，
不是单项独立贡献估计；未完成批次不能作为通过结果，最终以 manifest 为准。
全量验收还要求四个 profiling 计数逐任务不变：`observation_calls`、
`scalar_rejected_candidates`、`depth_materializations`、`candidate_cache_hits`。
生产匹配窗口、字段容差、缓存修订号、首次命中及收盘审计规则均不变。

本轮代码检查已通过 137 项 all-target/all-feature 测试、136 项默认 feature 测试、
文档测试、严格 Clippy 和格式检查；新增共享符号、spool 边界、标的状态隔离和惰性来源测试。

三个阶段的 24 次串行 ABBA 已完成：同市场语义报告跨阶段相等，四项计数也完全相等。
最终组合的小样本回调均值为 SH `0.3201 → 0.2986 s`、SZ `0.5562 → 0.4387 s`。
阶段 1 单独减少分配并未显示独立回放提速：SH `0.2750 → 0.2887 s`、
SZ `0.7726 → 0.8225 s`；最终组合的回放分项为 SH `0.2743 → 0.2752 s`、
SZ `0.7732 → 0.7954 s`。不能据此宣称每一项均单独加速。
小样本参考加载仍需扫描全市场文件，但仅构造少量选中标的的参考帧；
加载优化和整体吞吐应以全量回归为主要评估依据。

2026-09-09 全量回归完成：30/30 个日市场任务通过，30 份语义报告与 `0b4c37a` 完全相等。
414,714,148 个可比项全部匹配，553 个状态排除项保持不变；不匹配、数据错误及缺源均为零。
四项 profiling 计数逐任务完全相等，验收收据记录 `verified_counter_fields`；
本轮没有通过减少观察、跳过额外候选或调整缓存命中条件提速。
该轮验收时 Rust 源码与本批次冻结源码逐文件一致，输入清单与基线相同。
最终二进制与 stage3 一致，SHA256 为
`a095c7f0d52116c68c16f9fd04390a520aa57bc71de013d249c594d992dba765`。

同样使用六进程并发；下表是 30 个任务累计分项秒数，RSS 为单进程峰值的平均值：

| 分项 | 优化前（0b4c37a） | 三项优化后 | 变化 |
| --- | ---: | ---: | ---: |
| validation 总耗时 | 10,767.86 s | 9,707.17 s | 下降 9.85% |
| 其中：验证回调 | 8,483.52 s | 7,616.45 s | 下降 10.22% |
| 其中：参考快照加载 | 2,047.96 s | 1,865.94 s | 下降 8.89% |
| 其中：报告整理 | 236.38 s | 224.78 s | 下降 4.91% |
| 恢复链路（不含验证） | 16,153.77 s | 15,830.92 s | 下降 2.00% |
| 其中：输入分片 | 4,989.65 s | 4,722.69 s | 下降 5.35% |
| 其中：回放循环（不含验证） | 10,991.16 s | 10,942.07 s | 下降 0.45% |
| 平均峰值 RSS | 14.435 GiB | 14.445 GiB | 增加 0.07% |

沪市／深市 validation 累计耗时分别下降 4.81%／14.42%，回调分别下降 4.35%／15.32%。
29/30 个任务 validation 更快，28/30 个任务回调更快；并非每个任务均提速：
`20260601-SH` 的 validation 增加 1.08%、回调增加 3.14%，
`20260527-SH` 回调增加 0.47%，但 validation 下降 2.45%。
整批日历耗时从 84.36 分钟降至 79.69 分钟，不能与累计分项耗时混用。

评估结论：组合优化主要改善 validation 回调和参考加载，内存基本持平；
回放循环分项仅有 0.45% 的小幅变化，不能宣称核心恢复算法显著加速。
全量报告仍省略成功逐帧元数据；首次命中、失败原子性及诊断上下文另由单元／差分测试覆盖。
计时为同配置的两批 instrumented wall time，非独占 CPU 或多轮全量随机交错 A/B，
不能将报告整理等未直接修改部分的变化归因于该部分算法优化，也不能承诺固定硬件外的加速比。

## Profiling

验证回调在恢复过程中执行，不能另跑 replay 后将两次总时间相减作为精确对比耗时。
默认生产 API 不记录运行耗时；启用 `profiling` 后，
`profile_validate_market_day` 返回同一次验证的分项时间：

```bash
cargo build --release --locked --features profiling --example validation_benchmark
target/release/examples/validation_benchmark \
  --date 20260320 --market SZ --temp-root target/profile-spool \
  --report reports/manual-profile-sz.json \
  --timings reports/manual-profile-sz-timings.json
```

此 example 与 CLI 的证券默认值不同：省略 `--symbols` 表示全部支持的股票与 ETF。
深市使用实用市价策略，不提供扩窗开关。`--max-detail-records` 默认 5000，0 表示不限；
不保留成功逐帧明细，全部汇总计数不受明细上限影响。请使用新的报告路径，不覆盖已有证据。

| 分项 | 范围 |
| --- | --- |
| `reference_load_seconds` | raw snapshot 读取、阶段检查和验证器初始化 |
| `input_spool_seconds` | 逐笔读取、Schema、过滤、通道分片及逆序修复 |
| `replay_excluding_validation_seconds` | 回放循环耗时扣除本次实际验证回调 |
| `validation_callbacks_seconds` | 候选盘口取值、字段比较和收盘审计 |
| `spool_cleanup_seconds` | 成功分片清理 |
| `report_finalize_seconds` | 验证汇总和报告构造 |
| `report_serialization_write_seconds` | benchmark 调用者写验证 JSON，位于核心计时之外 |
| `scalar_rejected_candidates` | 标量差异界限允许跳过十档读取的候选数（仅 profiling） |
| `depth_materializations` | 实际构造十档用于比较的候选数（包括收盘投影，仅 profiling） |
| `candidate_cache_hits` | 普通候选使用相同盘口修订号复用视图的次数，仅 profiling；即使引用不同，也仍分别比较 |

```text
恢复 = input_spool + replay_excluding_validation + spool_cleanup
对比 = reference_load + validation_callbacks + report_finalize
profiled_total = 恢复 + 对比 + unattributed
```

恢复不是纯 OrderBook::apply CPU 时间；这些都是 wall time，包含调度和资源争用。
时钟埋点有开销，不做未经实测的扣除。外层 `/usr/bin/time -v` 还包含进程启动、报告／
timing JSON 写出和退出；失败时不返回伪造的完整分项，保留进程耗时及失败上下文。

两批最多六进程并发，逐日恢复、对比、总耗时和峰值内存见各 summary。一天两市的跨度
取最早启动至最晚结束，不等于两市耗时相加；累计耗时是工作量，不能当作并发日历耗时。
数据规模、并发负载、缓存和埋点不同，不能直接归因于算法性能提升。

## 恢复路径借用键查询优化（2026-09-09，后续增量）

在上述冻结版本之后，证券通道登记改用借用键 entry，仅首次登记分配 `String`；
盘口 runtime 查询改用一次 entry 查询，替代 `contains_key` 与 `get_mut` 双重查找。
两处与 validation 共用 `SymbolMap`，仍使用标准随机 hasher。
通道冲突的错误及原有覆盖行为、runtime 创建失败不插入、已有 runtime 不重建均保持不变。
没有修改恢复状态机、候选窗口或字段比较规则。

新增四项单元测试覆盖首次／重复登记、通道冲突、runtime 复用及创建失败后重试。
全部 feature 的 all-target 测试、严格 Clippy 和格式检查通过。

本增量全量验收已完成：`reports/20260909-p1-borrowed-lookup-full-regression/`。
使用 `analysis/run_borrowed_lookup_validation.py`，与上一轮 allocation 冻结结果比较，
不是与 `0b4c37a` 比较。新二进制 SHA256 为
`27df1c8a2cd902606a218e4bb6b1951ffb9aa9234f00ac57a25dcbe6b6db03d6`。
15 个相同交易日（20260127–20260828 的原验收日期）、沪深股票和 ETF、六进程，
75 个输入文件的路径、行数、Schema、大小和修改时间与基线一致；不是全文件内容哈希核验。
本轮运行时间为北京时间 2026-09-09 19:38:19–20:56:50。

30/30 个日市场任务通过，完整语义报告及四项 profiling 计数逐任务相等。
414,714,148 个可比项全部匹配；553 个状态排除项不变，不匹配、数据错误及缺源均为零。
沪市匹配 205,758,141、排除 259；深市匹配 208,956,007、排除 294。

下表为 30 个任务的累计分项时间，不是六进程批次跨度：

| 分项 | 上一轮 | 本轮 | 耗时变化 |
| --- | ---: | ---: | ---: |
| 输入读取与分片 | 4,722.69 s | 4,635.93 s | 下降 1.84% |
| 回放（扣除验证回调） | 10,942.07 s | 10,914.75 s | 下降 0.25% |
| 恢复合计（含分片清理） | 15,830.92 s | 15,733.54 s | 下降 0.62% |
| validation 合计 | 9,707.17 s | 9,776.16 s | 增加 0.71% |
| 恢复 + validation | 25,538.09 s | 25,509.70 s | 下降 0.11% |

按市场划分，每日平均时间：

| 市场 | 恢复：旧 → 新 | validation：旧 → 新 | 合计：旧 → 新 | 合计变化 |
| --- | ---: | ---: | ---: | ---: |
| SH | 417.34 → 411.08 s | 324.67 → 321.86 s | 742.00 → 732.95 s | 下降 1.22% |
| SZ | 638.06 → 637.82 s | 322.48 → 329.88 s | 960.54 → 967.70 s | 增加 0.75% |

输入分片有 25/30 任务变快；纯回放为 14/30，恢复和端到端各为 18/30。
沪市恢复下降 1.50%，深市恢复下降 0.04%；平均峰值 RSS 为 14.4449 → 14.4394 GiB，
基本不变。六进程批次跨度为 79.69 → 78.52 分钟，缩短 1.17 分钟（1.46%）；
调度重叠和尾部任务会影响跨度，不能把它代替累计延时收益。

结论：正确性全量回归通过，输入阶段改善方向较一致，但尚不能宣称有明显、稳定的
端到端提速。此次改动未修改 validation 算法；validation 的约 0.71% 波动不能直接
归因于借用键优化。该比较是两次历史六进程 wall time 运行，不是独占 CPU 的配对实验。
纯回放仍含解码、归一化和计时开销，不是单独 `OrderBook::apply` 延时；两轮均不写固定截面。

分项和逐日结果见该批次 `restore-comparison.json`、`comparison.json`、`*-full-run.json`。
`audit.ipynb` 从原始新旧报告与 timing 文件独立复算并通过所有断言；本机缺少 Jupyter，
其代码单元通过 Clara Python 按顺序执行并保存输出，而非通过 Jupyter kernel 执行。
当前 Rust 源码与本轮冻结源码一致；运行后仅整理驱动格式及更新证据文档，没有修改 Rust。

## 证据维护

当前调度／汇总入口见 [analysis/README.md](../analysis/README.md)。manifest、收据、源码
快照与 summary 配套保存；历史报告不因文档或代码更新自动变为新版本证据。
替换基线必须明确代码／输入指纹、覆盖范围、失败与排除计数及计时环境。

早期调查留在[归档](archive/real-data-validation-20260828-investigation.md)，
本地 `reports/README.md` 与 `reports/cleanup-20260908.json` 保存旧报告清理说明。
未推送提交中的报告内容已移除，本地文件保留并独立备份；不要依赖这些提交恢复报告。
已经发布的远程历史未重写，可能仍含旧报告。后续提交只保留本文汇总和复现工具，
详细产物通过独立存储维护，不强制加入 Git。
