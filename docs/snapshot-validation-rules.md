# 沪深股票与 ETF 的 Snapshot 验证匹配规则

本文是 snapshot 与逐笔恢复订单簿匹配规则的唯一规范来源。采用“规则速查 + 详细约束”
结构；正文描述目标行为，代码落实情况见[实现状态](#8-实现状态)。

命令与参数见[回放与验证手册](full-market-replay-guide.md)，实验、历史口径和差异分析见
[真实数据验证记录](real-data-validation.md)。历史报告不因规范更新而自动视为新规则的验收结果。

## 规则速查

参考源为 raw snapshot；`T` 为参考帧时间，所有候选窗口均左闭右开。

| 验证阶段 | 沪市股票、ETF | 深市股票、ETF |
| --- | --- | --- |
| 开盘集合竞价结束 | 正常 `OCALL → 首个 TRADE`，位于 `[09:25,09:30)`；窗口 `[T,T+1s)` | 正常 `O0 → 首个 B0`，尚未进入 `T0`；窗口 `[T,T+1s)` |
| 盘中交易 | 09:30 起的正常 `TRADE`，保留 `14:57:00 TRADE`；窗口 `[T-1s,T+1s)` | 正常 `T0`；主板、ETF 窗口 `[T,T+1s)`，创业板窗口 `[T,T+3s)` |
| 收盘集合竞价结束 | 首个正常 `CCALL → CLOSE` 参考帧，对比逐笔流首个有效 `CLOSE` 检查点状态 | 完整回放当日委托、成交／撤单，对比首条正常 `E0`；不按 `quote_time <=15:00` 截断 |

- 三个阶段使用统一完整字段集合；候选必须在同一状态同时满足所有字段。
- 停牌按阶段排除，复牌后正常阶段继续验证；`CCALL/C0` 不参与普通盘中盘口比较。
- 深市开盘同阶段重复 `B0`、两市重复 `CLOSE/E0` 必须内部一致。
- 深市出现 `quote_time >15:00:00.000` 时须报告并确认业务阶段，不能直接把独立盘后
  交易后的状态当作 E0 状态。收盘不使用候选窗口。
- 验证不改变原生序号回放，也不改变生产截面的严格 `quote_time < T` 语义。

## 1. 适用范围与数据来源

适用于完整单日 Parquet 的离线回放：一个交易日、一个市场，可选择股票、ETF 或二者。
股票和 ETF 共用流程、比较字段和异常分类；市场、板块和品类差异由明确的参数与规则表达。

| 市场 | 逐笔来源 | 独立参考 snapshot |
| --- | --- | --- |
| 沪市 | `mdl_4_24_0` 合并逐笔 | `MarketData` |
| 深市 | `mdl_6_33_0` 委托、`mdl_6_36_0` 成交／撤单 | `mdl_6_28_0` |

文件位于 `raw_level2_parquet/date=YYYYMMDD/<数据集>/part-0.parquet`。当前规范不使用
canonical snapshot 作为主验证源，也不读取 `.csv.7z` 冷归档执行生产验证。

## 2. 阶段选帧与收盘边界

### 2.1 开盘集合竞价结束

- 沪市：选择正常 `OCALL → TRADE` 转换后、`[09:25,09:30)` 内首条 `TRADE`。
- 深市：选择正常 `O0 → 首个 B0`，且此前尚未进入 `T0`。
- 两市均在 `[T,T+1s)` 内寻找候选状态。参考帧是整秒阶段时间，不能仅按同毫秒比较。
- 正常转换不得跨越停牌等异常阶段；“当天曾出现过 OCALL/O0”不足以证明正常转换。
- 深市重复 B0 检查仅限该正常开盘阶段、首次 T0 之前；不混入午间或其他阶段的 B0。
- 缺少正常开盘阶段时记录原因，不生成正常匹配结论；不影响之后正常盘中或收盘验证。

### 2.2 盘中交易

- 沪市：09:30 起的正常 `TRADE`，使用 `[T-1s,T+1s)`。保留 `14:57:00 TRADE`，
  不用 `<14:57:00` 硬截断参考帧。
- 深市：正常 `T0`，主板股票及 ETF 使用 `[T,T+1s)`，创业板股票使用 `[T,T+3s)`。
- `CCALL/C0` 仅标记收盘集合竞价阶段，不比较普通盘中盘口。
- 临时停牌后恢复正常交易的 `TRADE/T0` 重新纳入比较。异常阶段顺序必须报告，不能
  仅凭状态文字任意挑选参考帧。
- 时间窗口属于当前数据源验证契约，不是交易所通用时间规则；不得失败后自动扩窗。

### 2.3 收盘集合竞价结束

沪市：

1. 参考源选择正常 `CCALL → CLOSE` 后的首条 `CLOSE`。
2. 恢复器在对应通道按 `BizIndex` 处理到该证券首个有效逐笔 `CLOSE` 状态事件。
   原始标识为 `Type=S` 且 `TickBSFlag=CLOSE`；之前的有效委托、撤单、成交全部应用。
3. 状态事件本身不修改盘口。检查点由原生序列位置确定，不使用 snapshot 的时间戳或
   接收时间截断逐笔，也不按 `quote_time <=15:00` 判断。

深市：

1. 参考源选择首条正常 `E0`。
2. 完整读取当日委托和成交／撤单文件，按通道内 `ApplSeqNum` 合并回放；全部有效事件
   成功应用后，以最终盘口进行静态比较，不使用 `quote_time <=15:00:00.000` 筛掉输入。
3. 单独统计 `quote_time >15:00:00.000` 的记录。若出现，须确认业务阶段；未确认前
   不得宣告最终状态就是 E0 状态，更不能混入独立盘后交易后仍报告收盘验收通过。

共同约束：

- 收盘不使用候选窗口，也不要求收盘集合竞价实际有成交。
- 重复 `CLOSE/E0` 的规范化比较字段必须一致，不能挑选恰好匹配的一帧。
- 深市 E0 位于独立 snapshot 流，不是逐笔 `ApplSeqNum` 中的状态事件。离线文件读完
  与输入完整性是上述规则的前提；未来实时接入必须另有“逐笔已收齐”机制，不能收到
  E0 就宣告回放完成。

## 3. 候选状态匹配语义

开盘及盘中阶段统一采用以下规则：

1. 始终按市场原生序号回放，不按行情时间、接收时间或 snapshot 序号重排逐笔。
2. 检查规定窗口内成功应用事件形成的状态；窗口内无事件时，持续存在的盘口也可作为
   候选。窗口右端点上的事件不进入该窗口候选。
3. 存在一个候选状态，其全部规定字段同时满足比较要求，即判定参考帧匹配。
4. 不允许十档、总量和成交统计分别命中不同候选后拼接为匹配。
5. 失败事件不得改变订单簿或形成成功候选，回放错误必须独立报告。

记录命中候选时间及对应原生序号。时间对外使用毫秒精度；内部纳秒容器不代表源数据
具有纳秒测量精度。静态延续状态的候选时间与最后成功事件时间不是同一概念。

验证窗口与生产截面相互独立：

```text
Snapshot(T) = 按原生序号成功应用所有 quote_time < T 事件后的订单簿状态
```

## 4. 比较字段与数值口径

三个阶段使用同一组字段：

| 类别 | 比较内容 | 要求 |
| --- | --- | --- |
| 十档盘口 | 买卖十档价格、数量、每档委托数 | 规范化后精确一致 |
| 全簿汇总 | 买卖总委托量 | 精确一致 |
| 加权价格 | 买卖加权平均价格 | 按规定发布精度处理后，绝对误差 `<=0.001 元` |
| 成交价格 | 最新价、最高价、最低价 | 精确一致；深市收盘 LastPrice 按第 5 节处理 |
| 成交累计 | 成交笔数、成交量、成交额 | 统一整数单位后精确一致 |

- 内部价格乘数固定为 `10_000`，即一个单位为 `0.0001 元`；这不等于证券最小报价单位。
  成交额使用足够宽的整数类型，运算须检查溢出。
- Decimal 直接十进制转换，不经 Float64。确需兼容 Float64 时，使用已验证的舍入
  归一化方法，不恢复会误拒的旧误差阈值；不得通过降低精度掩盖真实差异。
- 零价或 null 价可规范为空档，但数量、委托数必须符合空档约束；非法组合报错，
  不能直接清零。
- 时间戳、接收时间、序号、状态、昨收和开盘价不作为普通盘口一致性字段。
  状态用于选帧，前收盘价可用于深市当日无成交的收盘价处理。

## 5. 深市收盘 LastPrice

股票和 ETF 共用以下决策流程，保留适用品类的价格取整单位差异：

| 情况 | 处理 |
| --- | --- |
| LastPrice 已一致 | 正常比较 |
| LastPrice 不一致，收盘集合竞价有成交 | 判定不匹配，不启用回退 |
| LastPrice 不一致，收盘集合竞价无成交，当日有成交 | 独立计算最后一笔成交前 60 秒至该笔成交（含端点）的 VWAP，按适用价格单位取整 |
| 当日无成交 | 使用前收盘价作为比较用收盘价 |

只有独立计算结果与参考 LastPrice 一致，且其他比较字段全部满足要求，才判定匹配并
记录规则标签。前收盘价等必要输入缺失时不能虚构数值。

该处理不修改订单簿内部最后成交价，不豁免股票或 ETF 的价格比较。沪市保留自身
LastPrice／ClosePrice 字段语义，不机械套用深市转换。

## 6. 停牌、复牌和参考帧质量

- `SUSP`、`H0/B1/T1/E1` 等状态排除对应阶段的正常验证，不排除整只证券全天。
- 复牌后的正常盘中及收盘帧继续比较；复牌竞价委托按对应阶段语义恢复，不能仅依据
  钟表时间套用普通连续竞价处理。
- 可以使用参考源状态辅助识别阶段，但不得以 snapshot 价量或成交统计补填订单簿，
  再反过来证明匹配。当前通联复牌适配依赖已核验的同时间戳批次契约，不推广为任意
  数据源的精确阶段边界。
- 重复静态帧检查范围为：深市正常开盘、首次 T0 前的 B0；沪市同一正常收盘阶段的
  CLOSE；深市同一正常收盘阶段的 E0。
- 重复帧的规范化业务字段须精确一致，不要求时间戳、接收时间或序号相同；不使用候选
  搜索，也不借用恢复比较的加权均价容差放行参考源内部差异。

## 7. 结果分类与验收边界

| 分类 | 含义 |
| --- | --- |
| 匹配 | 同一候选状态或规定最终状态满足全部比较要求 |
| 不匹配 | 存在合法参考及必要来源，但无法匹配 |
| 状态排除 | 对应阶段停牌或不存在正常可比阶段 |
| 数据异常／来源不足 | 非法档位、异常转换、重复帧冲突、应有数据缺失等 |

按“市场 × 股票/ETF × 阶段”报告匹配数、不匹配数、可比匹配率、排除与来源不足的数量、
原因及覆盖率；不匹配项附首个差异字段和必要事件上下文。另报深市收盘价规则标签及
超过收盘时间的事件数量。

状态排除不计作匹配。存在数据异常、来源不足时不能通过静默跳过获得验收通过。
全部可比项须满足规则；应有数据缺失仍需明确判定验收未完成或失败。

证据边界：

- 当前窗口主要由 20260828 实验支持，需跨日期回归，不得自动扩窗直到匹配。
- 沪市扩窗结果来自原全量成功集合与全部失败帧复验的合并，不冒充扩窗后重新全量扫描。
- 已核查 20260803、0814、0827、0828 深市逐笔最大行情时间不超过 15:00，因而这些日期
  完整回放与 `<=15:00` 截断纳入的事件集合相同；不保证其他日期永远如此。
- Snapshot 匹配证明规定的汇总盘口与成交统计一致，不单独证明订单身份、逐订单余量
  和 FIFO 正确；后者仍依赖逐订单对账及核心不变量测试。

## 8. 实现状态

截至 2026-09-05，本次为文档整理，不修改运行逻辑。中文阶段名称不代表已修改公共枚举
或报告字段：当前分别为 `PreOpen/pre_open`、`ContinuousTrading/continuous_trading`、
`MarketClose/market_close`。

| 项目 | 当前实现与目标差异 |
| --- | --- |
| 原生序号回放、统一字段比较、深市收盘价处理 | 已实现，继续保留 |
| 沪市逐笔 CLOSE 检查点 | 已实现 |
| 深市完整回放后比较 E0 | 待调整；当前仍在完整处理 `15:00:00.000` 时间档后捕获收盘状态 |
| 按市场／板块自动选择候选窗口 | 部分实现；沪市默认前后各 1 秒，深市默认前向 1 秒；创业板 3 秒当前需运行级参数，不自动按标的分流 |
| 正常阶段转换校验 | 部分实现；已有前置 OCALL/O0 等检查，仍需补齐异常中间阶段及“B0 前未进入 T0”约束；深市选帧仍附固定时间范围 |
| 重复静态帧 | 深市 E0 已检查；开盘 B0、沪市 CLOSE 检查待补齐 |
| 停牌、复牌处理 | 已有阶段原因及 H0→T0 复牌适配；完整状态排除台账仍待补齐 |
| 结果分类与超时事件统计 | 当前为 Matched/Mismatched/NotComparable 及原因；独立分类、深市 `>15:00` 专项诊断待落实 |
| 候选审计信息 | 已输出毫秒候选时间；对应命中原生序号的报告字段待补齐 |

上述待实现项落实后须补充合成测试、样本回归及相应全量验收，不能直接沿用旧报告认定通过。
